#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int a[10000], b[10000];
    for(int i=0;i<n;i++)
        cin>>a[i];
    for(int i=0;i<n+1;i++)
    {
        cin>>b[i];
    }
    for(int i=0;i<n+1;i++)
    {
       for(int j=0;j<n;j++)
        {
            if(b[i]==a[j])
                b[i]=-1;
        }
        if(b[i]!=-1)
        {
            cout<<b[i]; return 0;
        }
    }
}
