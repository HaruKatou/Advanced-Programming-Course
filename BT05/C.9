#include <iostream>
#include <math.h>
#include <stdlib.h>
#include <time.h>

using namespace std;
int random(int N) {
    return rand() % N;
}
int main()
{
    int n; cin>>n;
    srand(time(NULL));
    int ran_n = random(n);
    cout<<ran_n;
}
